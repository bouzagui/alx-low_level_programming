#include "main.h"

/**
 * mul - multiplication of two numbers.
 * @a: The first number.
 * @b: The second number.
 *
 * Return: The result of the multiplication.
*/
int mul(int a, int b)
{
    int result;

    result = a * b;
    return (result);
}
