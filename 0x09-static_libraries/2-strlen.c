#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - The length of the string.
 * @s: The string.
 * Return: The length of the string.
*/
int _strlen(char *s)
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
